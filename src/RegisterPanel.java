import javax.swing.*;

public class RegisterPanel extends JPanel {

    public RegisterPanel() {
        // Basic panel setup
        JLabel label = new JLabel("Register Panel");
        add(label);
    }
}
